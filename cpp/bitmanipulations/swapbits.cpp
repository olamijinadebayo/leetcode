#include<string>
#include <vector>
using namespace std;
#include<iostream>

// extract the two bits to be swapped by shiffting by both indexes to the right
//if they are different just toggle the bits