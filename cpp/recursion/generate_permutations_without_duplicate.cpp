#include <iostream>
#include <vector>
using namespace std;

// for each permutation
// for each iteration from i-n
// swap str[i] with str[l]
// recursively permutate the remaining part of the string
// backtrack by returning the string to its original position



int main(){
    string test= "AABC";
    vector<string>result;
    
    for (auto &&i : result)
    {
        cout<<i<<endl;
    }
    
    return 0;
}